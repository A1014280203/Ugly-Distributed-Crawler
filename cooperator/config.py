# settings for connecting redis-server
r_server = {
    'ip': "1.2.3.4",
    'port': '6379',
    'passwd': '',
    # the name of set which stores proxies
    's_name': 'proxy_ip'
}

# for time.sleep()
settings = {
    'interval': 5*60
}
